<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/statebarColor</item>
        <item name="colorPrimaryDark">@color/statebarColor</item>
        <item name="colorAccent">@color/fontBlack1</item>
        <item name="android:textColor">@color/fontBlack1</item>
    </style>

</resources>
